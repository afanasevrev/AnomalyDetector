package com.example.nads.repository;

import com.example.nads.domain.FlowFeatures;
import org.springframework.data.jpa.repository.JpaRepository;

public interface FlowFeaturesRepository extends JpaRepository<FlowFeatures, Long> {}
